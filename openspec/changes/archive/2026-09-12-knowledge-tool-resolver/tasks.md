## 0. Baseline (convenção 19)

- [x] 0.1 Rodar a suíte inteira em **worktree limpo, antes de qualquer edição**, e guardar a **saída completa** (não só o resumo — o nome do teste que reprova é o que se perdeu no fechamento da 2a): `apps/api`, `apps/inbox`, `apps/workers`, `tests/` e `apps/frontend`. Com Podman, exportar `DOCKER_HOST` conforme `docs/development.md#testcontainers-com-podman`
- [x] 0.2 **Medir a carga externa antes de CADA alvo**, não só antes do primeiro, e registrar o número ao lado de cada resultado — é o acréscimo que a 2a propôs à convenção 19 e que esta change exerce pela segunda vez. Limiar declarado: load prévio de 1 min < 5,0 e nenhum processo alheio ≥ 100%. Reprovação conhecida a esperar: o flake de ordem de execução de `AgentDeactivationTests`; **qualquer outra é anterior a esta change e precisa ser separada por escrito**
- [x] 0.3 `apps/workers`: registrar no baseline a contagem atual de classes em `WorkerHostCollection` (**11**) e o tempo da suíte, porque a tarefa 7 vai comparar contra esses números

## 1. Slugificador de nome de tool, movido (`apps/workers`)

- [x] 1.1 `apps/workers`: mover `AgentDelegations/DelegationToolNameSlugifier.cs` para `Naming/ToolNameSlugifier.cs`, renomeando o tipo para `ToolNameSlugifier`. **Movimento puro, sem mudar uma linha de comportamento** — é o segundo consumidor que a convenção 2 pede para extrair (design D3)
- [x] 1.2 `apps/workers`: atualizar `AgentDelegationToolSetResolver` e `DelegationToolNameSlugifierTests` para o tipo movido. **Não alterar nenhuma asserção do teste**: se ele passar como está, o movimento está correto (design R11). Renomear o arquivo de teste para `ToolNameSlugifierTests.cs`
- [x] 1.3 `apps/workers`: no XML doc do tipo movido, registrar que ele tem **dois** consumidores (delegação e conhecimento) e que a remoção de diacríticos via `FormD` é o que distingue este slugificador da sanitização de `ToolNameSanitizer` — os dois rodam em sequência e fazem coisas diferentes

## 2. Resolvedor de tools de conhecimento (`apps/workers`)

- [x] 2.1 `apps/workers`: criar `Knowledge/Execution/IKnowledgeToolSetResolver.cs` com `Task<IReadOnlyList<AITool>> ResolveAsync(AppDbContext dbContext, Guid agentId, CancellationToken ct)`. **A assinatura é a de `IMcpToolSetResolver`, não a de delegação** — não são necessários `sourceAgent`, `contextId`, `currentDepth` nem `messageInstant` (design V1). XML doc registrando que o `dbContext` é reaproveitado do escopo de quem chama, mesmo padrão dos outros dois
- [x] 2.2 `apps/workers`: criar `Knowledge/Execution/KnowledgeSearchResult.cs` — record com `Documento` (o `Title` do catálogo), `Trecho` e `Distancia`. **Três campos, e `headingPath` não é um deles**: o chunker já injeta o caminho de cabeçalhos no próprio texto vetorizado (design D2/V9). XML doc registrando isso, e registrando que o `Title` do catálogo **difere** do `#` do markdown que está dentro do trecho — é o único nome que o operador controla
- [x] 2.3 `apps/workers`: criar `Knowledge/Execution/KnowledgeToolDescription.cs` — monta a descrição da tool a partir da `Description` da base mais o bloco fixo que ensina a ler a distância. **Nenhum dígito de corte** (design D4). XML doc registrando que um limiar em prosa seria o limiar reprovado por `0c` com outro nome, e sem o benefício de ser testável
- [x] 2.4 `apps/workers`: criar `Knowledge/Execution/KnowledgeToolSetResolver.cs` — consulta `AgentKnowledgeBases` × `KnowledgeBases` com `where kb.IsActive`, `orderby binding.KnowledgeBaseId` (determinismo, e por id e não por nome porque o nome é editável — mesmo motivo da Decisão 8 de `0a`), e monta uma `AITool` por linha via `AIFunctionFactory.Create`, no molde de `AgentDelegationToolSetResolver.BuildDelegationTool`
- [x] 2.5 `apps/workers`: nome da tool via `ToolNameSanitizer.Sanitize($"search_{ToolNameSlugifier.Slugify(nome)}")`. **Sem dedupe local** — quem garante unicidade é o `ToolNameDeduplicator`, e manter os dois seria dois mecanismos para a mesma invariante (design D8). Comentar citando o defeito que `0a` corrigiu
- [x] 2.6 `apps/workers`: no corpo da tool, resolver o gerador de embedding **por invocação** (`embeddingResolver.Resolve()`), nunca na montagem do conjunto (design D9), e gerar o vetor da consulta com `GenerateVectorAsync` — a extensão de entrada única de `Microsoft.Extensions.AI` (design V4). **Não descartar o gerador**, mesmo tratamento de `KnowledgeIndexingService`
- [x] 2.7 `apps/workers`: a consulta em LINQ, não SQL cru (design D10/V2) — `join` com `KnowledgeDocuments` pelo `Title`, `where f.KnowledgeBaseId == baseId`, `orderby f.Embedding.CosineDistance(q)`, projetando `Distance = f.Embedding.CosineDistance(q)`, `Take(5)`. O `5` é **constante nomeada** com XML doc citando a regra de `0c` (R@3 = 92,2% de R@5, abaixo dos 95% que exigiriam `k=3`) e o custo medido (~1.150 caracteres por trecho, ~5,2 KB por chamada)
- [x] 2.8 `apps/workers`: **caso da base sem fragmento** (design D5) — contar antes de buscar, ou distinguir pelo resultado vazio, e devolver texto dizendo que a base ainda não tem conteúdo indexado. **Nunca "a busca não encontrou nada relacionado"**: comentar que os dois estados são distinguíveis e que conflatá-los é a convenção 13 na direção forte
- [x] 2.9 `apps/workers`: degradação graciosa na invocação (convenção 4, spec "Falha na busca degrada a tool") — `catch` que devolve resultado de falha ao modelo e registra log com agente e base, sem derrubar a task. Mesmo idioma de `AgentDelegationToolSetResolver.DelegateToTargetAsync`
- [x] 2.10 `apps/workers`: registrar `IKnowledgeToolSetResolver` no `Program.cs` como singleton, junto dos outros dois resolvedores

## 3. Terceiro conjunto no namespace de tool (`apps/workers`)

- [x] 3.1 `apps/workers`: acrescentar `Knowledge` a `Mcp/ToolOrigin.cs`, e atualizar o XML doc do enum, que hoje fala em dois conjuntos
- [x] 3.2 `apps/workers`: `Mcp/ToolNameDeduplicator.cs` — quarto parâmetro `knowledgeTools`, terceiro `AppendAll`. **A ordem dos parâmetros É a precedência declarada** (design D8): MCP → delegação → conhecimento. Atualizar o XML doc do método, que hoje diz "os dois conjuntos"
- [x] 3.3 `apps/workers`: atualizar o XML doc da classe `ToolNameDeduplicator`, que descreve o namespace como "a união das tools MCP com as tools de delegação"
- [x] 3.4 `apps/workers`: `Agents/AgentExecutionService.cs` — injetar `IKnowledgeToolSetResolver`, resolver **depois** das tools de delegação, e passar o terceiro conjunto ao deduplicador
- [x] 3.5 `apps/workers`: **sem `await using` no conjunto de conhecimento** (design D7), com comentário curto dizendo o porquê e apontando para o comentário grande do `chatClient` logo acima — o sítio já tem uma assimetria documentada e esta change a reforça em vez de contrariá-la. **Não tocar** no comentário de `fix-vazamento-httpclient-chat`

## 4. Testes do resolvedor (`apps/workers`)

- [x] 4.1 `apps/workers`: criar `Knowledge/KnowledgeToolSetResolverTests.cs`, `IClassFixture<WorkerInfrastructureFixture>` no molde de `McpToolSetResolverTests` — testa o resolvedor direto, sem `AgentExecutionService`. As entidades espelho são read-only, então o arranjo semeia por **SQL cru**, mesmo padrão de lá. **Exige Postgres real**: `AppDbContext` ignora `KnowledgeFragment` fora de Npgsql (design V8)
- [x] 4.2 `apps/workers`: duplo de `IEmbeddingGeneratorResolver` que devolve vetor determinístico, para que a distância seja previsível e as asserções não dependam de chamada de rede. **Dimensão 4096**, a mesma da coluna
- [x] 4.3 `apps/workers`: cenários de resolução — base vinculada e ativa produz uma tool; **base vinculada e inativa não produz**; duas bases produzem duas tools; agente sem vínculo não produz nenhuma
- [x] 4.4 `apps/workers`: **guarda de base inativa visto reprovar, e no arranjo certo** (design R3, convenção 15 primeira forma) — a base inativa do cenário precisa estar **vinculada e com fragmentos gravados**, senão o teste mede ausência de conteúdo e não o filtro. Remover o `where kb.IsActive` e ver reprovar; recolocar e ver passar
- [x] 4.5 `apps/workers`: cenários de busca — devolve no máximo 5 em ordem crescente de distância; base com 2 fragmentos devolve 2; **fragmento de outra base não vaza** (duas bases vinculadas, ambas povoadas)
- [x] 4.6 `apps/workers`: **par determinístico da ordenação** (design R2, convenção 15 quinta forma) — capturar o `Executed DbCommand` do log do EF Core durante a invocação **real** da tool (nunca uma consulta remontada no teste, convenção 11) e afirmar que o `ORDER BY` usa a expressão de distância e que há `LIMIT`. O guarda comportamental de ordem sozinho é insuficiente por construção, porque o Postgres às vezes devolve ordenado sem o `ORDER BY`
- [x] 4.7 `apps/workers`: cenários do resultado — os três campos presentes; **asserção negativa** de que não há campo de relevância/acerto/confiança; consulta sobre assunto ausente da base **devolve trechos com distância mesmo assim** (design R1)
- [x] 4.8 `apps/workers`: **asserção negativa sobre a `Description`** (design D4/R1) — a descrição gerada não contém dígito apresentado como corte de distância. É o guarda que reprova a regressão bem-intencionada de "deixar mais útil"
- [x] 4.9 `apps/workers`: **o par da convenção 5 com as duas pontas do "sem item"** — base sem nenhum fragmento informa "sem conteúdo indexado"; base **com** fragmentos e consulta irrelevante **não** informa isso e devolve trechos. Os dois cenários juntos são o que impede conflatá-los
- [x] 4.10 `apps/workers`: cenário de falha — gerador de embedding que lança; a tool devolve resultado de falha, a task conclui, o log nomeia agente e base

## 5. Testes de integração e de namespace (`apps/workers`)

- [x] 5.1 `apps/workers`: criar `Knowledge/KnowledgeToolExecutionEndToEndTests.cs` com `[Collection(WorkerHostCollection.Name)]`, no molde de `McpToolExecutionEndToEndTests` — pipeline real (`TaskJobConsumer`/`AgentExecutionService`), agente com base vinculada, o LLM falso chamando a tool de conhecimento de verdade e o resultado voltando para a resposta
- [x] 5.2 `apps/workers`: cenário que prova o fim do vazio da linha — **um agente com base vinculada e índice povoado responde citando conteúdo que só existe na base**. É o cenário que o teste manual da 5a-2 não conseguiu produzir, e é o que fecha a etapa
- [x] 5.3 `apps/workers`: `Agents/AgentToolNamespaceTests.cs` — acrescentar os cenários do terceiro conjunto: duas bases de nomes que slugificam igual; colisão entre conhecimento e MCP; o aviso de log nomeando a origem `conhecimento`; estabilidade da ordem das tools de conhecimento entre execuções
- [x] 5.4 `apps/workers`: **guardas de colisão vistos reprovar no componente que a correção toca** (design R4, convenção 15 segunda forma) — os cenários de 5.3 reprovam no `ToolNameDeduplicator`, não no resolvedor novo. `0a` errou exatamente isso três vezes; conferir reintroduzindo a ausência do terceiro `AppendAll`

## 6. Custo de DI nos harnesses (`apps/workers` e `tests/`)

- [x] 6.1 `apps/workers`: criar `Support/NullKnowledgeToolSetResolver.cs`, no molde exato de `NullAgentDelegationToolSetResolver`, com XML doc apontando onde fica a cobertura real
- [x] 6.2 `tests/InboxOrchestratorRoundTrip.Tests`: criar o `Support/NullKnowledgeToolSetResolver.cs` daquele projeto — duplicado de propósito, mesmo padrão dos outros dois nulos, porque os projetos não se referenciam
- [x] 6.3 `apps/workers`: registrar o nulo nos **12 harnesses** que constroem `AgentExecutionService` — `AgentDelegationConcurrencyTests`, `AgentDelegationExecutionTests`, `Agents/AgentToolNamespaceTests`, `ChannelContextMessageTests`, `ConversationHistoryTests`, `HistorySummarizationTests`, `Mcp/McpToolExecutionEndToEndTests`, `Notifications/PushNotificationEndToEndTests`, `TaskJobConsumerTests` (dois sítios), `TemporalContextMessageInstantTests`, `WorkerTests`. **Duas linhas cada, e nenhuma delas é trabalho de teste** — é o custo de DI que a régua de `0a` manda contar antes (design V11)
- [x] 6.4 `tests/InboxOrchestratorRoundTrip.Tests`: registrar o nulo em `Support/RoundTripFixture.cs`
- [x] 6.5 Nos harnesses que **exercitam** conhecimento (5.1, 4.x), registrar o resolvedor **real**, não o nulo

## 7. Fechamento da suíte e limiar de carga (todos os apps)

- [x] 7.1 Rodar a suíte inteira com o **mesmo procedimento de carga** do baseline — `uptime` antes de cada alvo, espaçamento entre eles, nada em paralelo —, e comparar alvo a alvo contra 0.1/0.2. Guardar a **saída completa**
- [x] 7.2 `apps/workers`: registrar a contagem nova de `WorkerHostCollection` (**12**, de 11) e o tempo da suíte contra o do baseline
- [x] 7.3 **Recalibrar o limiar de carga — tarefa desta change, não descoberta dela.** Está declarado para **7** classes e já reprovou 1/212 com carga dentro dele em 11 (design R6); esta change o leva a 12. Medir o número novo, com a **saída completa guardada**. É a aplicação direta da **convenção 22**, que esta change promoveu: quem acrescenta a classe recalibra o limiar
- [x] 7.3.1 Escrever o número novo nas **duas metades que a convenção 22 exige**, e conferir que as duas estão lá: (a) **o estado ao lado do número**, na mesma frase — "load prévio < X, medido com 12 classes de host", nunca "load prévio < X"; (b) **o gatilho de recalibração apontando para condição observável** — "recalibrar quando entrar classe nova em `WorkerHostCollection`", nunca "quando fizer sentido". Sem gatilho pendurado, a referência quebra uma terceira vez pelo mesmo motivo pelo qual quebrou a segunda
- [x] 7.4 Se alguma reprovação aparecer, **não classificar como "ambiental" sem a baseline** (convenção 19). Baseline verde acusa regressão; baseline vermelha não absolve ninguém. Capturar o **nome** do teste — foi o que se perdeu na 2a e tornou a dúvida irrecuperável

## 8. Verificação manual (`apps/workers`)

- [x] 8.1 Subir a stack de desenvolvimento e mandar uma mensagem para o agente *Atendente Ambiente Software*, que é o único com base vinculada no banco de dev (*Gestor de Agentes*, 272 fragmentos, 2 documentos indexados). Conferir no log que a tool de conhecimento foi resolvida e chamada
- [x] 8.2 Conferir que a resposta cita conteúdo que **só** existe na base — os dois documentos indexados são o `01` e o `02` do próprio repositório, então uma pergunta sobre uma convenção específica serve
- [x] 8.3 **Rodar de novo o censo de nome de tool** (design V7) contra o banco de dev, agora com a change aplicada, e conferir que o inventário real bate com o que a consulta SQL previu. É inventário, não validação do dedupe
- [x] 8.4 **Gatilho vencido de OUTRA change, não trabalho desta.** A tarefa 8.2 de `fix-vazamento-httpclient-chat` deixou a verificação manual do vazamento pendente e apontou o gatilho para "a validação manual da etapa 4, que roda no mesmo caminho de execução" — é aqui. Contar descritores (`lsof -p <pid> | wc -l`) antes e depois de uma sequência de mensagens. **Precisa ser com Gemini ou Anthropic**: com OpenAI a medição não mostra nada nem antes nem depois, porque aquele SDK usa `HttpClient` estático (C4), e rodar só com OpenAI produziria um "corrigido" vazio — **NÃO EXECUTADA**: exige credencial de Gemini ou Anthropic, que não existe neste ambiente. Destino cumprido pela 8.4.1
- [x] 8.4.1 **Se a 8.4 NÃO rodar por falta de credencial, o destino é reabrir no `02-HISTORICO_E_STATUS.md`, não deixar registrado aqui.** `tasks.md` some no archive: um item marcado "pendente" dentro de um arquivo arquivado morre exatamente como o `Purpose` placeholder do `4d` e como o nome de teste que a saída filtrada da 2a perdeu. Reabrir como item próprio na lista de abertos, dizendo que o gatilho de `fix-vazamento-httpclient-chat` **foi alcançado e não pôde ser exercido**, e por quê
- [x] 8.4.2 **O gatilho novo precisa de POSIÇÃO, não só de condição.** "Quando houver chave" já falhou nesta jornada — é a família *trabalho adiado sem lugar na fila*, com três ocorrências registradas. A forma verificável: **"a próxima change que tocar `apps/workers` com credencial de provedor não-OpenAI disponível"**, com a observação de que `Anthropic__ApiKey`/`Gemini__ApiKey` **não chegam a processo nenhum no `docker-compose.prod.yml`** (item aberto próprio) — então a condição depende de um segundo item, e isso precisa estar escrito, senão o gatilho aponta para algo que ninguém agendou

## 9. Documentação (tarefa própria por artefato, convenção da 2a)

- [x] 9.1 `01-ARQUITETURA_E_CONVENCOES.md`: o conjunto de tools entregue ao LLM passa a ser **três** conjuntos, com a precedência estendida. Conferir o modelo de domínio (`AgentKnowledgeBase` ganha o primeiro leitor). **Conferir a convenção 15** — se algum guarda desta change produzir forma nova, registrar; se não, dizer que foi conferido
- [x] 9.2 `01-ARQUITETURA_E_CONVENCOES.md`: **já decidido antes do apply — a convenção 22 foi escrita, e a quarta ocorrência é a recalibração da tarefa 7.3.** O que sobra aqui é **exercê-la**, não decidi-la: o número novo do limiar sai de 7.3 **com o estado ao lado** ("load < X, medido com 12 classes de host") e **com o gatilho de recalibração** ("recalibrar quando entrar classe nova na coleção"), que é exatamente o que a convenção 22 passa a exigir de todo número medido. Se a redação de 7.3 não tiver as duas metades, ela não cumpre a convenção que esta change acabou de promover
- [x] 9.2.1 `02-HISTORICO_E_STATUS.md`: conferir que os dois itens de gatilho de promoção **já fechados antes do apply** continuam coerentes depois da change — o da convenção 22 (promovido) e o de *trabalho adiado sem lugar na fila* (não promovido, continua com três e ganhou gatilho próprio). **Se esta change produzir uma quarta ocorrência da segunda família, é aqui que ela se decide**, pelo mesmo critério: decidir, não adiar por omissão
- [x] 9.3 `01-ARQUITETURA_E_CONVENCOES.md`: registrar, como orientação de operação e **não** como requisito, o que `0d` mediu sobre `Description` de base — duas bases cujas descrições se contêm roteiam mal de forma assimétrica, a genérica engolindo a específica (design R5). O item completo já está no `02`, com o que fazer, o sintoma a procurar e onde encosta na tela; aqui vai a forma curta, ao lado da descrição de `KnowledgeBase` no modelo de domínio, que é onde alguém lê "obrigatória e não vazia" e conclui que não-vazia basta
- [x] 9.4 `02-HISTORICO_E_STATUS.md`: a change, com a **medição da convenção 18** decomposta por categoria (criados/modificados × produção/teste), contra a projeção de ~26 arquivos / ~1.470 linhas. Registrar se a régua de DI de `0a` acertou os 15 arquivos
- [x] 9.5 `02-HISTORICO_E_STATUS.md`: fechar o item "Etapa 4" da fila, promover a busca unificada para a posição seguinte, e conferir se o item do vazamento (8.4) fecha ou continua aberto
- [x] 9.6 `docs/architecture.md`: **a linha 330 diz "união de dois conjuntos"** e a seção inteira de namespace de tool descreve dois. Passa a três. Conferir também a seção de bases de conhecimento
- [x] 9.7 `README.md`: **a linha 161 diz que "the retrieval tool the agent would call does not exist yet"** — deixa de ser verdade com esta change. Conferir também a lista de capacidades (linha ~82), que descreve MCP e delegação
- [x] 9.8 `CHANGELOG.md`: entrada em `[Unreleased]`, seção `Added`, no grupo **Bases de conhecimento** que já existe. Formato Keep a Changelog, pt-BR
- [x] 9.9 `docs/configuration.md`: conferir se a seção `Embedding` precisa dizer que o modelo passa a ser usado **também** na consulta, por mensagem, e não só na indexação. Se não precisar, dizer que foi conferido
- [x] 9.10 Rodar `python3 scripts/check-docs.py` e ver passar — lembrando que ele valida **estrutura, não verdade**: frase que virou mentira passa verde, e foi assim que a 2a deixou três afirmações falsas
- [x] 9.11 Escrever o `Purpose` real de `knowledge-tool-execution` e conferir o de `agent-tool-namespace` (hoje começa com `TBD - defined by change dedupe-global-nome-de-tool`) — **antes do archive**, nesta passada. O estoque de placeholders está em 36 e o skill continua escrevendo o placeholder sozinho
