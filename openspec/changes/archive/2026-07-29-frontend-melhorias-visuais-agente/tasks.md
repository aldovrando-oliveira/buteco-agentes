## 1. apps/frontend — dependências

- [x] 1.1 (apps/frontend) Adicionar `react-markdown@10.1.0` e `remark-gfm@4.0.1` às `dependencies` de `package.json` e instalar.

## 2. apps/frontend — AgentForm: botão Cancelar

- [x] 2.1 (apps/frontend) Em `AgentForm.tsx`, adicionar `onCancel: () => void` como prop obrigatória em `AgentFormProps` e um botão "Cancelar" (`variant="default"`) ao lado do botão de submit, no mesmo `Group`/`Stack`, chamando `onCancel` ao clicar — sem validação do formulário, sem diálogo de confirmação.
- [x] 2.2 (apps/frontend) Em `AgentCreatePage.tsx`, passar `onCancel={() => navigate('/agents')}` ao `AgentForm`.
- [x] 2.3 (apps/frontend) Em `AgentEditPage.tsx`, passar `onCancel={() => navigate(\`/agents/${id}\`)}` ao `AgentForm`.
- [x] 2.4 (apps/frontend) `AgentForm.test.tsx`: novo caso cobrindo os Scenario "Cancelar o cadastro" / "Cancelar a edição" no nível de `AgentForm` — clicar em "Cancelar" chama `onCancel`, sem chamar `onSubmit`. Não cobre o destino da navegação (isso é responsabilidade de cada página, ver 2.6/2.7).
- [x] 2.5 (apps/frontend) Confirmar que `AgentCreatePage.test.tsx` e `AgentEditPage.test.tsx` continuam passando sem alteração no fluxo de submit já existente — `onCancel` é uma prop nova repassada a `AgentForm`, não altera o comportamento de submit já coberto por esses arquivos.
- [x] 2.6 (apps/frontend) `AgentCreatePage.test.tsx`: novo caso cobrindo o destino da navegação do Scenario "Cancelar o cadastro" — clicar em "Cancelar" navega para `/agents` (mockar `useNavigate` e afirmar o argumento da chamada, mesmo padrão já usado para testar a navegação de sucesso neste arquivo).
- [x] 2.7 (apps/frontend) `AgentEditPage.test.tsx`: novo caso cobrindo o destino da navegação do Scenario "Cancelar a edição" — clicar em "Cancelar" navega para `/agents/{id}` com o id correto do agente em edição (mockar `useNavigate` e afirmar o argumento da chamada, mesmo padrão já usado para testar a navegação de sucesso neste arquivo).

## 3. apps/frontend — AgentDetailPage: reposicionamento do bloco de ações

- [x] 3.1 (apps/frontend) Em `AgentDetailPage.tsx`, mover o `Group` com Editar/Ativar/Desativar para antes de `<AgentDetailCard agent={data} />` no JSX.
- [x] 3.2 (apps/frontend) `AgentDetailPage.test.tsx`: novo caso cobrindo o Scenario "Bloco de ações exibido antes dos dados do agente" — confirmar via ordem no DOM (ex.: `compareDocumentPosition` ou comparação de índice entre os elementos retornados por `screen.getAllBy...`) que o link/botão "Editar" precede o heading do nome do agente.

## 4. apps/frontend — AgentDetailCard: renderização markdown com altura limitada

- [x] 4.1 (apps/frontend) Em `AgentDetailCard.tsx`, substituir `<Text>{agent.instructions}</Text>` por um `ScrollArea` (`h="calc(100vh - 320px)"`, `mih={220}`) envolvendo `Typography` envolvendo `<ReactMarkdown remarkPlugins={[remarkGfm]}>{agent.instructions}</ReactMarkdown>`, importando `Typography` e `ScrollArea` de `@mantine/core`, `ReactMarkdown` de `react-markdown` e `remarkGfm` de `remark-gfm`. Altura ajustada de um valor fixo (`mah={400}`) para altura explícita ligada à viewport com piso mínimo, após feedback visual real (ver Decision 1 revisada em `design.md`).
- [x] 4.2 (apps/frontend) Criar `AgentDetailCard.test.tsx` (arquivo novo, seguindo o padrão dos demais testes da feature — `MantineProvider theme={theme}` como wrapper): cobrir o Scenario "Instruções renderizadas como markdown formatado" — instruções com um título markdown (`# Nome`) renderizam como elemento de heading real via `getByRole('heading', ...)`, não como texto literal `"# Nome"`.
- [x] 4.3 (apps/frontend) `AgentDetailCard.test.tsx`: cobrir lista e negrito renderizando como elementos reais (ex.: `getByRole('list')`, texto em `<strong>`) e uma tabela via `getByRole('table')`, já que `remark-gfm` foi confirmado necessário para conteúdo real.
- [x] 4.4 (apps/frontend) `AgentDetailCard.test.tsx`: teste estrutural cobrindo os Scenario "Bloco de instruções usa o espaço vertical disponível, com rolagem quando excede" e "Bloco de instruções mantém altura mínima utilizável" — confirmar que o `ScrollArea` é renderizado envolvendo o conteúdo markdown com `height` e `min-height` aplicados (verificável via estilo inline no elemento raiz do `ScrollArea`; `height` explícito, não `max-height` — ver 4.7). Não prova rolagem/layout visual real (jsdom não calcula layout), só que o componente e a configuração certos estão no lugar certo — a rolagem visual em si é validada manualmente em 5.3.
- [x] 4.5 (apps/frontend) Confirmar que os testes existentes em `AgentDetailPage.test.tsx` que usam `screen.getByText(activeAgent.instructions)` (fixture de texto plano sem sintaxe markdown) continuam passando após a troca de `<Text>` para `react-markdown` — texto plano deve renderizar como nó único de texto dentro do `<p>` gerado.
- [x] 4.6 (apps/frontend) Ajuste pós-feedback visual (1ª rodada): trocar a altura fixa do `ScrollArea` (`mah={400}`) por altura mínima + altura máxima relativa à viewport (`mih={220}`, `mah="calc(100vh - 320px)"`), atualizando `AgentDetailCard.test.tsx` e `design.md`/`specs/agent-catalog-ui/spec.md`.
- [x] 4.7 (apps/frontend) Correção de bug real (2ª rodada, encontrado ao validar 5.3 em browser real): `mah` sozinho não bastava — o `Viewport` interno do `ScrollArea` do Mantine usa `height: 100%`, que só resolve contra uma altura **explícita** do pai; com só `max-height` (altura própria `auto`), o `Viewport` crescia para caber todo o conteúdo e a rolagem ficava inerte, mesmo com o `Root` cortando visualmente o excedente. Trocado `mah="calc(100vh - 320px)"` por `h="calc(100vh - 320px)"` (mantendo `mih={220}`); `AgentDetailCard.test.tsx` atualizado para verificar `height` em vez de `max-height`; `design.md` documenta a causa raiz confirmada via estilos computados em Chromium real.

## 5. apps/frontend — validação final

- [x] 5.1 (apps/frontend) Rodar a suíte completa de testes (`npm test`) e confirmar que todos os testes, novos e existentes, passam.
- [x] 5.2 (apps/frontend) Rodar lint e build (`npm run lint`, `npm run build`) e confirmar que não há erros introduzidos pelas novas dependências ou pela mudança de tipo em `AgentFormProps`.
- [x] 5.3 (manual) Validar manualmente em browser real que (a) o container de instruções rola internamente sem que a página cresça além do necessário, e (b) os botões Editar/Ativar/Desativar permanecem visíveis no topo, sem precisar rolar a página inteira. Docker não estava disponível neste ambiente para subir `apps/api` completo (Postgres/RabbitMQ), então a validação foi feita com `npm run dev` em `apps/frontend` + Chromium real via Playwright (`chromium.launch`), interceptando a chamada `GET /agents/{id}` para devolver um agente mock com um prompt longo (múltiplos `#`/`##`, listas, negrito, mirando a estrutura real do agente "Triagem" mostrada pelo usuário). Confirmado via `boundingBox`/estilos computados e uma sequência de 2 screenshots (antes/depois de um `mouse.wheel`) que o scroll move o conteúdo (`viewportScrollTop` foi de `0` para `600` após o wheel) e os botões de ação permanecem fixos no topo, fora da área de rolagem. Foi essa validação que revelou o bug corrigido em 4.7 — a primeira tentativa (`mah`) passava nos testes jsdom mas falhava em browser real.
