## 1. Correção de produção (apps/inbox)

- [x] 1.1 Em `PushNotificationEndpoints.DeliverResponseAsync`, mover o início do `try` para antes da consulta que resolve `dispatchInfo` (Session → Contact → Channel), cobrindo em sequência essa consulta, a construção de `outboundMessage` (incluindo `credentialCipher.Decrypt`), a resolução do sender e `sender.SendAsync` — `occurredAt = DateTimeOffset.UtcNow` permanece antes do `try` (design.md, Decisão 1).
- [x] 1.2 Ajustar a mensagem de log do `catch` para identificar a falha por `pendingDispatch.SessionId` em vez de `dispatchInfo.Id`/`dispatchInfo.ChannelType` (podem não existir se a própria consulta falhar) — manter `catch (Exception exception)` único e amplo, sem lista fechada de tipos (design.md, Decisões 2 e 3).
- [x] 1.3 Conferir que `dbContext.Messages.Add(MessageEntity.CreateOutbound(pendingDispatch.SessionId, responseText, occurredAt, MessageDeliveryStatus.Failed, exception.Message))`, no `catch`, continua correto para os três pontos de falha possíveis agora cobertos (consulta, decrypt, sender) — nenhum desses argumentos vem de `dispatchInfo` (design.md, Context), então nenhum dos três precisa de campos que só existiriam em caso de sucesso da etapa anterior.

## 2. Testes (apps/inbox — Buteco.Inbox.Tests)

- [x] 2.1 Em `PushNotificationEndpointsTests.cs`, adicionar um `Fact` par de `ReceiveAsync_SenderThrows_PersistsOutboundMessageAsFailedWithReasonAndDoesNotFailRequest`: semear o canal com um `EncryptedCredentials` inválido (string que não passou por `cipher.Encrypt`, mesmo padrão de literal usado por `RoundTripTests.CreateChannelAsync`) e asserir que (a) a resposta HTTP é `200 OK`, (b) o `PendingDispatch` é removido, (c) a `Message` de saída é persistida com `MessageDeliveryStatus.Failed` e motivo não vazio, (d) `TestOutboundMessageSender` não recebeu nenhuma mensagem para essa sessão — convenção 5 (asserção explícita, não "processado corretamente" genérico). Distinção deliberada do uso do mesmo literal em `RoundTripTests`: lá é fixture forjado por acidente, mascarou o defeito real desta change por várias sessões, e será corrigido em `roundtrip-fixture-credencial-real`; aqui é deliberado — forçar exatamente essa falha de decrypt é o ponto do teste, não um desvio de convenção 11 (que rege acordo entre dois lados reais, não este teste de resiliência local a `apps/inbox`).
- [x] 2.2 Rodar a suíte completa de `apps/inbox` (`Buteco.Inbox.Tests`) e confirmar 100% verde, incluindo o teste novo e o já existente `ReceiveAsync_SenderThrows_...` (par "com item"/"sem item" da convenção 5 continua coberto pelos testes de sucesso já existentes na classe).

## 3. Verificação (apps/inbox)

- [x] 3.1 Confirmar que `tests/InboxOrchestratorRoundTrip.Tests` **não** é usado para verificar esta change (Non-Goal do design.md — continua parado pela ausência do fixture correto) e que nenhuma tarefa desta lista depende dele passando.

## 4. Documentação (raiz do repo)

- [x] 4.1 Em `02-HISTORICO_E_STATUS.md`, corrigir o item do timeout de `tests/InboxOrchestratorRoundTrip.Tests` registrando as três leituras na ordem em que ocorreram (i. "latência marginal do podman, ~21-22s contra limite de 20s"; ii. "não completa nem com 90s, causa não aprofundada, ambiental"; iii. causa real — regressão de produção introduzida em `9fd8a87`, último bom `c72c64f` passando em 1s) e por que as duas primeiras leituras enganaram — registrar como lição de método, não só o fato.
- [x] 4.2 No mesmo registro, explicar por que os bisects anteriores (que compararam `a91f0c9`/HEAD e leram "idêntico nos dois lados" como sinal de causa ambiental) não pegaram a regressão: `9fd8a87` é ancestral de `a91f0c9`, então toda comparação base×HEAD daquelas sessões estava dentro da janela já quebrada — "reproduz no commit base escolhido" não descarta regressão quando esse base já vem depois do commit ruim.
- [x] 4.3 Registrar a hipótese do `DebounceSweepService` (engolir exceção silenciosamente) como testada e descartada nesta exploração, com a evidência (log completo sem nenhuma das duas mensagens de erro esperadas) — para não ser reaberta em sessões futuras.
- [x] 4.4 Fechar a pergunta do Docker nativo: confirmado ausente neste ambiente (sem binário, sem Docker.app, sem contexto) — o item em aberto que citava "faltaria comparação nativa, não disponível" deixa de ser incerteza de método, porque a causa nunca foi ambiental.
- [x] 4.5 Atualizar o item da asserção de convenção 11 de `inbox-instante-mensagem`
  (`MessageReceived_TriggersFullRoundTrip_TaskCarriesMessageInstantInRawPersistedJson`, nunca executada) com o gatilho concreto: assim que esta change **e** `roundtrip-fixture-credencial-real` estiverem aplicadas e o round-trip voltar a completar, rodar esse teste especificamente antes de qualquer outro trabalho na linha de contexto temporal/canal.
- [x] 4.6 Registrar `roundtrip-fixture-credencial-real` como próxima change (fixture de `RoundTripTests.CreateChannelAsync` usando `IChannelCredentialCipher.Encrypt` real em vez do literal `"irrelevante-nesta-fatia"`), com a consequência de ordem escrita: só depois desta change de produção, nunca antes.
- [x] 4.7 Em `01-ARQUITETURA_E_CONVENCOES.md`, avaliar se a convenção 4 ganha uma nota generalizando o padrão de defeito observado três vezes (`DebounceSweepService`; e agora `PushNotificationEndpoints.DeliverResponseAsync`) — o `try/catch` certo existe, mas a chamada que mais realisticamente falha está posicionada fora dele, e isso não é exclusivo de `BackgroundService` (o texto atual da convenção 4 só fala de `BackgroundService`). Escrever a conclusão qualquer que seja — inclusive se a decisão for não alterar o texto, registrar o porquê.
- [x] 4.8 Em `02-HISTORICO_E_STATUS.md`, registrar como item em aberto (não desta change, ver design.md, Non-Goals/Open Questions): o bloco final e incondicional de `PushNotificationEndpoints.ReceiveAsync` (`UpdateMessageDispatchStatusesAsync` + `Remove(pendingDispatch)` + `SaveChangesAsync`, linhas 94-97 — fora de `DeliverResponseAsync`, roda para toda push notification aceita, com resposta ou sem) não tem `try/catch` próprio; uma indisponibilidade real de Postgres nesse ponto deixa o `PendingDispatch` preso e a exceção escapa, mesmo depois desta change. Diferente de um `PendingDispatch` `Pending`, um `Dispatching` preso por essa via não é revisitado pela próxima varredura de `DebounceSweepService` (filtro `Status == PendingDispatchStatus.Pending`). Gatilho: próxima falha real de Postgres observada afetando `apps/inbox`, ou próxima change que precisar tocar `ReceiveAsync` por outro motivo — o que vier primeiro.
