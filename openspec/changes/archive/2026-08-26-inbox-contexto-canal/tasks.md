## 1. apps/inbox — escrita das chaves novas em Message.Metadata

- [x] 1.1 (apps/inbox) Em `DebounceSweepService.BuildSendMessageRequest`, gravar `Message.Metadata["channelType"]` com o valor de `Channel.ChannelType` da sessão de origem, serializado com `JsonSerializer.SerializeToElement(value, A2AJsonUtilities.DefaultOptions)` — mesmo helper já usado por `messageInstant`.
- [x] 1.2 (apps/inbox) No mesmo método, gravar `Message.Metadata["contactExternalId"]` com o valor de `Contact.ExternalId` da sessão de origem, com o mesmo helper de serialização — chave escalar separada, nunca agrupada com `channelType` num objeto (design.md, D3).
- [x] 1.3 (apps/inbox) Testes em `DebounceSweepServiceTests` (ou arquivo equivalente já existente): `SendMessage` disparado inclui `channelType` correto; inclui `contactExternalId` correto; as duas chaves são `JsonElement` do tipo string, nunca um objeto agrupando as duas.

## 2. apps/workers — bloco de contexto de canal

- [x] 2.1 (apps/workers) Criar `ChannelContextBlockBuilder.cs` (novo arquivo, mesmo espírito de `TemporalContextBlockBuilder` mas separado — design.md, D5), com um marcador próprio de "não é mensagem do usuário" e um método `Build(string? channelType, string? contactExternalId)` que retorna:
  - `null`/vazio quando os dois parâmetros são nulos ou vazios (design.md, D6, caso 1);
  - só a linha do campo presente quando só um dos dois está disponível (design.md, D6, caso 2), nomeando `contactExternalId` como "identificador do contato atribuído pelo canal", nunca como telefone (design.md, D4);
  - as duas linhas quando ambos estão disponíveis (design.md, D6, caso 3).
- [x] 2.2 (apps/workers) Em `AgentExecutionService`, adicionar um método privado de extração de `channelType`/`contactExternalId` da última mensagem do usuário no histórico da task (mesmo estilo de `ExtractMessageInstant`), por campo: `Message.Metadata` nulo, chave ausente, ou `JsonValueKind.String` com valor vazio (`""`) → ausência silenciosa, sem log; `JsonValueKind` diferente de `String` (número, objeto, array, booleano, `null`) → tratado como ausência para o bloco, **e** registra log de nível aviso identificando a task e o nome do campo (design.md, D6 — mesmo padrão de `ExtractMessageInstant` para valor ilegível, não um caso novo de instrumentação).
- [x] 2.3 (apps/workers) Concatenar o bloco de contexto de canal às instruções chamando `TemporalContextBlockBuilder.Concatenate` uma segunda vez, sobre o resultado da concatenação do bloco temporal — `Concatenate(Concatenate(agent.Instructions, temporalBlock), channelBlock)` — sem alterar a assinatura ou o corpo de `TemporalContextBlockBuilder` (design.md, D5, preserva o Non-Goal).
- [x] 2.4 (apps/workers) Testes em `ChannelContextBlockBuilderTests.cs` (novo): bloco completo com os dois campos; bloco parcial só com `channelType` — **com asserção negativa de que o texto resultante não contém nenhuma menção a `contactExternalId` (nem "desconhecido", nem placeholder)**; bloco parcial só com `contactExternalId` — mesma asserção negativa espelhada para `channelType` ausente; nenhum bloco (`null`/vazio) quando os dois estão ausentes; o texto do bloco nomeia `contactExternalId` como identificador atribuído pelo canal, nunca como telefone.
- [x] 2.5 (apps/workers) Testes em `ChannelContextMessageTests.cs` (novo, mesmo estilo de `TemporalContextMessageInstantTests.cs`): chamada ao LLM inclui o bloco de contexto de canal quando as duas chaves estão presentes na última mensagem do usuário; nenhum bloco quando ambas ausentes; bloco parcial quando só uma presente; o bloco não é persistido em `Agent.Instructions`.
- [x] 2.6 (apps/workers) Testes em `ChannelContextMessageTests.cs`: `channelType` ou `contactExternalId` gravado com `JsonValueKind` diferente de `String` (ex. número, objeto) é tratado como ausência do respectivo campo (bloco reflete a ausência, igual ao caso de chave não mandada) **e** emite log de nível aviso identificando a task e o nome do campo — par de casos, um por campo; distinguido explicitamente do caso de ausência silenciosa (metadata nula), que não loga.

## 3. Teste de acordo real entre apps/inbox e apps/workers (convenção 11)

- [x] 3.1 (tests/InboxOrchestratorRoundTrip.Tests) Adicionar cenário afirmando que `channelType` e `contactExternalId` gravados por `apps/inbox` sobrevivem, em JSON bruto persistido em `a2a_tasks.payload`, com o mesmo valor e formato (string, camelCase) até a leitura por `apps/workers` — usando o valor real produzido pelo `DebounceSweepService`, não um fixture forjado nos dois lados (convenção 11).

## 4. Registro

- [x] 4.1 (docs) Atualizar `02-HISTORICO_E_STATUS.md`: registrar esta change como aplicada, encerrando a linha de trabalho de contexto (temporal + canal); registrar D1 (`Contact.DisplayName` fora do prompt) como decisão com gatilho, incluindo os achados que a sustentam e a alternativa da saudação montada em código; registrar a distinção `ExternalId`/`ChannelType` (atribuídos pelo provedor) versus `DisplayName` (texto livre do usuário final) como achado reutilizável além desta change; registrar a superfície de dano do `AllowedTools` (não distingue leitura de escrita) como item em aberto próprio, com gatilho; registrar que sanitização por classe de caractere não é defesa contra injeção semântica; registrar que `NotAUserMessageMarker` não tem teste cobrindo, como item em aberto pequeno com gatilho.
- [x] 4.2 (docs) Avaliar, em `01-ARQUITETURA_E_CONVENCOES.md`, se cabe uma linha nova sobre o que entra na janela de contexto do agente (a distinção entre dado estrutural atribuído pelo provedor e texto livre do usuário final) — escrever a conclusão, mesmo que seja "não adicionar", com o motivo.
