## 1. Domínio e persistência (`apps/api`)

- [x] 1.1 `apps/api`: criar `KnowledgeBases/Entities/KnowledgeBase.cs` — `Id`, `Name`, `Description`, `IsActive`, `CreatedAt`, `UpdatedAt`, com construtor e métodos `UpdateDetails`/`Activate`/`Deactivate` no molde de `McpServer` (setters privados, sem setter público)
- [x] 1.2 `apps/api`: criar `KnowledgeDocuments/Entities/KnowledgeIndexingStatus.cs` — enum com os quatro valores `Pending`/`Indexing`/`Indexed`/`Failed`, anotado com `[JsonConverter(typeof(JsonStringEnumConverter<KnowledgeIndexingStatus>))]` (convenção 12, mesmo padrão de `McpServerAuthType`)
- [x] 1.3 `apps/api`: criar `KnowledgeDocuments/Entities/KnowledgeDocument.cs` — `Id`, `KnowledgeBaseId`, `Title`, `SourceType` (string), `ExtractedText`, `ContentLengthBytes` (int, **setter privado, nunca escrito pela aplicação**), `IndexingStatus`, `IndexedAt?`, `FailureReason?`, `ContentRevision` (int, inicia em 1), `CreatedAt`, `UpdatedAt`. Todos com setter privado, no mesmo molde de `KnowledgeBase`/`McpServer`; método de atualização que incrementa `ContentRevision` **apenas** quando `ExtractedText` muda, coloca `IndexingStatus = Pending`, limpa `FailureReason` e **preserva** `IndexedAt` (design D7/D9)
- [x] 1.4 `apps/api`: mapear as duas entidades no `AppDbContext` — tabelas `knowledge_bases` e `knowledge_documents`, FK de documento para base com **`OnDelete(DeleteBehavior.Restrict)`** (não o `Cascade` default do EF Core; design D6 — base não tem rota de exclusão, e `Restrict` obriga quem for adicioná-la um dia a decidir explicitamente o destino dos documentos), índice em `KnowledgeBaseId`, `IndexingStatus` persistido com `HasConversion<string>()` (nunca ordinal, nem no banco), e `ContentLengthBytes` mapeado com `HasComputedColumnSql("octet_length(\"ExtractedText\")", stored: true)` — o EF gera o DDL nativamente, **sem SQL cru na migração** (design D14, verificado por spike)
- [x] 1.5 `apps/api`: gerar a migração `AddKnowledgeBaseCatalog` e conferir o SQL gerado — aditivo puro, nenhuma tabela ou coluna existente tocada

## 2. Extrator e checagem de startup (`apps/api`)

- [x] 2.1 `apps/api`: criar `KnowledgeDocuments/Extraction/IKnowledgeSourceExtractor.cs` e `ExtractionResult.cs` — contrato que recebe o conteúdo cru e devolve texto extraído ou motivo de rejeição
- [x] 2.2 `apps/api`: criar `KnowledgeSourceTypes.cs` com a lista declarada de tipos suportados (`markdown`, único nesta etapa)
- [x] 2.3 `apps/api`: implementar `MarkdownSourceExtractor` — remove BOM UTF-8, normaliza `CRLF`/`CR` para `LF`, rejeita conteúdo com U+0000, rejeita conteúdo vazio ou só de espaços; **preserva a marcação markdown** (design D11 — a fragmentação da etapa 2 depende dos cabeçalhos)
- [x] 2.4 `apps/api`: registrar o extrator via `AddKeyedSingleton<IKnowledgeSourceExtractor>("markdown")` em `Program.cs`, no molde dos adapters de canal de `apps/inbox`
- [x] 2.5 `apps/api`: implementar `ValidateKnowledgeExtractorRegistrations` como extensão de `IServiceCollection`, chamada sobre `builder.Services` **antes** do `builder.Build()` — molde de `ValidateChannelAdapterRegistrations` (apps/inbox), não de `ValidateTimeZoneConfiguration`: a checagem inspeciona **descritores de DI keyed**, que é o que `IServiceCollection` expõe; sobre o host seria preciso resolver os serviços para descobrir as chaves. **Bidirecional**: tipo declarado sem extrator registrado falha o boot, e extrator registrado para tipo não declarado também falha (convenção 8). *O texto original desta tarefa dizia `host.Build()`/`host.Run()` e estava errado — corrigido durante a implementação, ver design.md D12.*

## 3. Endpoints e CQRS de base (`apps/api`)

- [x] 3.1 `apps/api`: `CreateKnowledgeBase` (command, handler, request, response) — valida nome e descrição não vazios, base nasce ativa
- [x] 3.2 `apps/api`: `UpdateKnowledgeBase` — mesmas validações, 404 para id inexistente
- [x] 3.3 `apps/api`: `ActivateKnowledgeBase` e `DeactivateKnowledgeBase` — idempotentes, 404 para id inexistente
- [x] 3.4 `apps/api`: `ListKnowledgeBases` e `GetKnowledgeBaseById` — incluem bases inativas; lista vazia responde 200, nunca 404
- [x] 3.5 `apps/api`: `KnowledgeBaseEndpoints` registrando `POST /`, `GET /`, `GET /{id}`, `PUT /{id}`, `POST /{id}/activate`, `POST /{id}/deactivate` — **sem rota de exclusão** (design D6); todas sob autenticação de operador, nenhuma entrada nova na allowlist anônima

## 4. Endpoints e CQRS de documento (`apps/api`)

- [x] 4.1 `apps/api`: criar `Options/KnowledgeDocumentLimits.cs` com o teto de 1 MiB (1.048.576 bytes UTF-8) como constante de produto — não configurável (convenção 2, sem cenário de outro valor)
- [x] 4.2 `apps/api`: `CreateKnowledgeDocument` — valida base existente (404), título não vazio, `sourceType` registrado (400 listando os suportados); **aplica o extrator ANTES de validar o teto**, e valida o teto sobre o texto já extraído (design D5 — é a mesma string que a coluna gerada mede; validar a entrada crua faria os dois números divergirem), com 400 e `ValidationProblemDetails`, nunca deixando o 413 sem corpo do Kestrel chegar ao cliente; persiste com `Pending`/`indexedAt: null`/`contentRevision: 1`
- [x] 4.3 `apps/api`: `UpdateKnowledgeDocument` — mesmo shape do create, mesmas validações; incrementa revisão só se o conteúdo mudou, volta para `Pending`, limpa `FailureReason`, preserva `IndexedAt`
- [x] 4.4 `apps/api`: `DeleteKnowledgeDocument` — exclusão real, 204 no sucesso, 404 para id inexistente
- [x] 4.5 `apps/api`: `ListKnowledgeDocuments` — projeta `KnowledgeDocumentSummaryResponse` **sem** `extractedText`, expondo a coluna gerada `contentLengthBytes`, nunca materializando o texto. **Não** usar `.Select(d => d.ExtractedText.Length)`: traduz para `length()`, que conta caracteres, e divergiria da unidade do teto (design D14)
- [x] 4.6 `apps/api`: `GetKnowledgeDocumentById` — inclui `extractedText` **e todos os campos da listagem, inclusive `contentLengthBytes`**, para que a tela de edição não precise de segunda requisição; 404 quando o id existe mas pertence a outra base
- [x] 4.7 `apps/api`: **conferir que os handlers de `Get`, `Update` e `Delete` filtram por `KnowledgeBaseId` E `Id`**, nunca só por `Id` — caso contrário é possível editar ou excluir documento de outra base pelo caminho errado, e o `DELETE` é irreversível
- [x] 4.8 `apps/api`: `KnowledgeDocumentEndpoints` registrando `POST`, `GET`, `GET /{id}`, `PUT /{id}` e `DELETE /{id}` sob `/knowledge-bases/{kbId}/documents` — **primeiro `MapDelete` do repositório**, com o comentário apontando para a decisão D6 do `design.md`; todas sob autenticação de operador

## 5. Espelho em `apps/workers`

- [x] 5.1 `apps/workers`: criar `Knowledge/Entities/KnowledgeBase.cs`, `KnowledgeDocument.cs` e `KnowledgeIndexingStatus.cs` como espelho das entidades de `apps/api` (mesmo padrão já usado para `McpServer`, sem `ProjectReference` cruzado, sem lib compartilhada)
- [x] 5.2 `apps/workers`: mapear as duas entidades no `AppDbContext` com exatamente a mesma configuração de `apps/api`
- [x] 5.3 `apps/workers`: gerar a migração `AddKnowledgeBaseCatalog` equivalente e conferir que o SQL bate com o de `apps/api` — incluindo o `Restrict` da FK para a base
- [x] 5.4 `apps/workers`: **nenhuma outra mudança** — nada em `AgentExecutionService`, nos resolvedores de tools ou no consumidor de fila (design D15)
- [x] 5.5 `apps/workers`: conferir que a migração nova **não** é aplicada por nenhum caminho de runtime nem entra em bundle — `grep` por `MigrateAsync`/`GetPendingMigrations`/`EnsureCreated` em `apps/workers/src` deve continuar sem resultado, e `deploy/migrate/Dockerfile` deve continuar buildando bundle só de `apps/api` e `apps/inbox`. É o guarda do risco R11; o mecanismo e o erro reproduzido (`42P07: relation "agents" already exists`) estão em D15
- [x] 5.6 **Nunca rodar `dotnet ef database update` a partir de `apps/workers`** contra banco real ou de dev — migração de banco sai só de `apps/api`. Se for preciso validar o schema espelhado, é contra Testcontainer limpo (tarefa 8.1)

## 6. Testes de `apps/api` (Testcontainers real, convenção 5)

- [x] 6.1 `apps/api`: `KnowledgeBaseCatalogTests` — criar (com/sem nome, com/sem descrição, nome duplicado), listar (com bases, sem nenhuma base, incluindo inativa), consultar (existente, inexistente, inativa), editar (sucesso, 404, descrição vazia), ativar/desativar (sucesso, idempotente, 404)
- [x] 6.2 `apps/api`: `KnowledgeDocumentCatalogTests` — criar (sucesso, base inexistente, base inativa, sem título, conteúdo vazio, título duplicado), listar (com documentos, base sem documentos, base inexistente), consultar (existente, inexistente, documento de outra base)
- [x] 6.3 `apps/api`: teste do teto de tamanho — conteúdo exatamente no limite é aceito; conteúdo acima responde 400 com `ValidationProblemDetails` e não cria registro (par "com item"/"sem item")
- [x] 6.4 `apps/api`: `KnowledgeDocumentUpdateTests` — conteúdo diferente incrementa a revisão e volta para `Pending`; só título não incrementa; conteúdo idêntico não incrementa; `IndexedAt` preservado; 404 para id inexistente; **404 para documento que pertence a outra base, afirmando que o documento da outra base ficou inalterado**; **atualizar documento de base inativa é permitido**; conteúdo acima do teto deixa documento e revisão intactos
- [x] 6.5 `apps/api`: `KnowledgeDocumentDeleteTests` — excluir existente responde 204 e some da listagem e da consulta; excluir inexistente responde 404; **excluir documento que pertence a outra base responde 404 e o documento da outra base continua existindo** (cenário mais caro de errar, porque a exclusão é irreversível); **excluir documento de base inativa é permitido**; excluir um não afeta os demais; excluir o último deixa a base com listagem vazia
- [x] 6.6 `apps/api`: `MarkdownSourceExtractorTests` — cabeçalhos e listas sobrevivem; BOM e `CRLF` normalizados; conteúdo com U+0000 rejeitado com 400 (sem chegar ao banco); acentos e emoji preservados; conteúdo só de espaços rejeitado; `.txt` sem marcação aceito
- [x] 6.7 `apps/api`: teste de `sourceType` desconhecido — 400 com `ValidationProblemDetails` listando os tipos suportados, nenhum registro criado
- [x] 6.8 `apps/api`: teste do ciclo de vida — documento nasce `Pending` com `indexedAt` nulo e **permanece** `Pending` numa consulta posterior (design D10 — estado esperado, não defeito)
- [x] 6.9 `apps/api`: `KnowledgeWireFormatTests` — inspecionar o **texto bruto** do JSON da resposta real (nunca desserializar para o mesmo tipo, convenção 11) e afirmar que `indexingStatus` é `"Pending"` e `sourceType` é `"markdown"` como string, não número. Registrar no próprio teste que `Indexing`/`Indexed`/`Failed` não são verificáveis nesta etapa por não terem escritor (design D10) — a extensão do teste é tarefa da etapa 2
- [x] 6.10 `apps/api`: teste de projeção da listagem — asserção **negativa** de que o JSON da listagem não contém a chave `extractedText`, e positiva de que o `GET` por id contém (design D14)
- [x] 6.11 `apps/api`: testes de `contentLengthBytes` — (a) criar documento com conteúdo acentuado cujo número de bytes UTF-8 difere do de caracteres e afirmar que o valor devolvido é o de **bytes**; (b) afirmar que **depois de um `PUT` que muda o conteúdo** o valor acompanha o texto novo, sem `Reload()` explícito (é o banco que recalcula — design D14); (c) documento com BOM e `CRLF` cujo tamanho bruto passa do teto mas cujo texto extraído não é aceito, e o valor devolvido é o do extraído (risco R12)
- [x] 6.12 `apps/api`: teste de autenticação — requisição sem token para rotas de base e de documento responde 401 e nada é executado

## 7. Testes da checagem de startup (`apps/api`, convenção 15)

- [x] 7.1 `apps/api`: **fixture de host não é viável, e isso é achado, não atalho** (design.md, D12 e nota da árvore de pastas): a checagem roda sobre `IServiceCollection` **antes** do `Build()`, e uma `WebApplicationFactory` só injeta serviços depois que `Program.cs` já registrou os seus e já rodou o validador — nenhuma das duas divergências seria observável por aquele caminho. Os testes exercitam `ValidateKnowledgeExtractorRegistrations` diretamente sobre uma `ServiceCollection`, que é exatamente como `Program.cs` a chama
- [x] 7.3 `apps/api`: `KnowledgeProductionRegistrationTests` — captura a `IServiceCollection` **real** pelo `ConfigureServices` da `WebApplicationFactory` (que roda depois dos registros de `Program.cs`) e afirma que os extratores registrados correspondem a `KnowledgeSourceTypes.All`. Fecha a lacuna da forma `IServiceCollection` da checagem: com a chamada removida do `Program.cs` **e** um registro `"pdf"` divergente, os testes de unidade ficam 4/4 verdes e estes dois reprovam (design.md, D12)
- [x] 7.2 `apps/api`: `KnowledgeExtractorStartupValidationTests` — registro completo permite o boot; tipo declarado **sem** extrator registrado falha com `InvalidOperationException` identificando o tipo; extrator registrado para tipo **não declarado** também falha (os dois sentidos, convenção 8)
- [x] 7.4 `apps/api`: **verificar que o guarda reprova de verdade** — reintroduzir cada uma das duas divergências de propósito, rodar, ver o teste falhar, e só então manter a implementação correta (convenção 15: guarda não verificado é pior que nenhum)

## 8. Testes de `apps/workers`

- [x] 8.1 `apps/workers`: `KnowledgeSchemaMirrorTests` — aplicar as migrações de `apps/workers` num Postgres de Testcontainers e afirmar que as tabelas e colunas geradas correspondem às de `apps/api`: nomes, tipos, nulidade, **FK documento→base com `Restrict`** (não cascata — design D6), índice em `KnowledgeBaseId`, e a coluna gerada `ContentLengthBytes` com a mesma expressão `octet_length`. Fecha o risco R9 de divergência entre os dois `AppDbContext`

## 9. Fechamento

- [x] 9.1 Rodar a suíte inteira de `apps/api` e `apps/workers` com Testcontainers real. **Duas variáveis são necessárias nesta máquina, não uma**: `DOCKER_HOST` apontando para o socket do Podman **e** `TESTCONTAINERS_RYUK_DISABLED=true` — sem a segunda, o resource reaper (Ryuk) tenta bind-mountar o socket do host num container e o Podman no macOS responde `operation not supported`, derrubando **toda** classe que usa Testcontainers com erro de infraestrutura disfarçado de falha de teste. Resultado: `apps/workers` 132/132; `apps/api` 235/236, com a única falha sendo flake de ordenação pré-existente em `AgentDeactivationTests` (passa isolada; registrada em `02-HISTORICO_E_STATUS.md` como carve próprio)
- [x] 9.2 Tamanho entregue: **62 arquivos / 2972 linhas escritas à mão** (mais 4 arquivos / 786 linhas de migração gerada). Acima da projeção de ~30-40 / ~1600-2200, e o motivo é a **âncora errada**, não implementação inflada: `b5df504` (30/1593) cobria **uma** entidade com dois campos e nenhum ponto de extensão, enquanto esta change tem **duas** entidades com CRUD completo (7 comandos/queries × ~2,3 arquivos pela convenção de um arquivo por comando/handler/result), mais a capability de extrator com checagem de startup (6 arquivos), mais 11 arquivos de teste cobrindo 66 cenários. A âncora certa era `da31b27` (catálogo + vínculo MCP: 76 arquivos / 3847 linhas) — e a entrega ficou **abaixo** dela
- [x] 9.3 Registrar em `02-HISTORICO_E_STATUS.md` os **cinco** itens de handoff, cada um com o destino: (a) o `.env` local com `ChatClient__*` obsoleto que não binda em `GetSection("OpenAI")` — change própria; (b) o rodapé do modal do protótipo que promete atomicidade que o desenho não sustenta — etapa 5a, convenção 17; (c) o contador do modal que conta caracteres enquanto o teto é em bytes UTF-8 — etapa 5a (design D14); (d) confirmação antes de excluir documento — etapa 5a, contraparte de produto para o risco R6, que não tem contraparte técnica nesta change; (e) **a regra operacional das migrações de `apps/workers`** — ver 9.3.1
- [x] 9.3.1 Registrar em `02-HISTORICO_E_STATUS.md`, como regra operacional durável (o `tasks.md` desaparece no archive), que **migração de banco real sai só de `apps/api`** — nunca rodar `dotnet ef database update` a partir de `apps/workers`. Incluir a evidência para que a próxima pessoa não precise reproduzir: contra um Postgres limpo, `apps/api` aplica com sucesso e `apps/workers` em seguida falha com `42P07: relation "agents" already exists`; o `__EFMigrationsHistory` fica com **apenas os IDs de `apps/api`**, porque a tentativa reverte inteira e não deixa linha — a falha é ruidosa e não corrompe estado. **Gatilho para revisitar:** antes de qualquer mudança que toque as migrações de `apps/workers`, ou se um segundo app passar a compartilhar o mesmo banco. Não há checagem de integridade barata viável aqui (nada em runtime consulta migrações em `apps/workers`), e por isso registro é a resposta — não prosa em lugar de guarda, mas o único instrumento aplicável (convenção 8)
- [x] 9.4 Registrar que a **etapa 2 herda decididas** as três garantias do fluxo de reindexação (design D9: substituição integral; fragmentos antigos sobrevivendo até o sucesso; falha preservando os antigos). Elas foram deliberadamente deixadas **fora** da spec desta change por não terem gatilho verificável aqui, e entram como *ADDED requirements* da etapa 2 com cenário real. A etapa 2 não as redecide; se divergir, corrige D9 com a causa (convenção 9)
- [x] 9.5 Conferir que nenhuma decisão do `design.md` divergiu na implementação; se alguma divergiu, corrigir o `design.md` com a causa real (convenção 9), nunca deixar só no resumo
